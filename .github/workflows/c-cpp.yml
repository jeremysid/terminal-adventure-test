name: C/C++ CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:

    runs-on: artix

    steps:
    - uses: actions/checkout@v3
    - name: main
      run: make ma
    - name: unstable
      run: make up
    - name: stable
      run: make sta
    - name: clean
      run: make clean
